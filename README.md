# p2p-chat-system
A Peer-to-Peer messaging system using TCP sockets with a Django-based STUN server for peer discovery, friendship management, and message storage. Includes Dockerized backend support.
